category: baseline
source: wget://http://ftp.gnu.org/gnu/make/make-3.82.tar.gz
satisfy_deb: (make >= 3.75)
#satisfy_rpm: (make >= 3.75)
configure {
    ./configure --prefix=$prefix
}
make {
    sh ./build.sh
}
install {
    ./make install
}
uninstall {
    ./make uninstall
}
